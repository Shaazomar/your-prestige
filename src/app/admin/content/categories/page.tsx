import { ModuleStub } from "@/components/admin/ModuleStub";

export const metadata = { title: "Categories" };

export default function Page() {
  return (
    <ModuleStub
      title="Categories"
      note="Organise the catalogue tree. Backed by the Category model."
    />
  );
}
