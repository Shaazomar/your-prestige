import { ModuleStub } from "@/components/admin/ModuleStub";

export const metadata = { title: "Homepage Editor" };

export default function Page() {
  return (
    <ModuleStub
      title="Homepage Editor"
      note="Edit every hero, section and CTA on the homepage. The Setting and Seo tables in the schema back this module."
    />
  );
}
