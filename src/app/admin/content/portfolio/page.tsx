import { ModuleStub } from "@/components/admin/ModuleStub";

export const metadata = { title: "Portfolio" };

export default function Page() {
  return (
    <ModuleStub
      title="Portfolio"
      note="Publish projects with type, timeline, galleries and video. Backed by the Project model."
    />
  );
}
