import { ModuleStub } from "@/components/admin/ModuleStub";

export const metadata = { title: "Brands" };

export default function Page() {
  return (
    <ModuleStub
      title="Brands"
      note="Manage the 40+ partner houses, logos and featured placement. Backed by the Brand model."
    />
  );
}
