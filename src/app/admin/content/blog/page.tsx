import { ModuleStub } from "@/components/admin/ModuleStub";

export const metadata = { title: "Journal" };

export default function Page() {
  return (
    <ModuleStub
      title="Journal"
      note="Write and publish SEO-optimised articles. Backed by the Post model with per-post SEO."
    />
  );
}
