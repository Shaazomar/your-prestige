import { ModuleStub } from "@/components/admin/ModuleStub";

export const metadata = { title: "Offers" };

export default function Page() {
  return (
    <ModuleStub
      title="Offers"
      note="Publish seasonal privileges with validity windows. Backed by the Offer model."
    />
  );
}
