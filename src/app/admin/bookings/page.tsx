import { ModuleStub } from "@/components/admin/ModuleStub";

export const metadata = { title: "Bookings" };

export default function Page() {
  return (
    <ModuleStub
      title="Bookings"
      note="Showroom visit calendar view of leads with type=VISIT — confirm, reschedule and assign consultants."
    />
  );
}
