import { ModuleStub } from "@/components/admin/ModuleStub";

export const metadata = { title: "Analytics" };

export default function Page() {
  return (
    <ModuleStub
      title="Analytics"
      note="Visitors, conversion funnels, top pages and popular products — plug in your analytics provider in Settings."
    />
  );
}
