import { ModuleStub } from "@/components/admin/ModuleStub";

export const metadata = { title: "SEO Studio" };

export default function Page() {
  return (
    <ModuleStub
      title="SEO Studio"
      note="Per-page titles, descriptions, canonicals, OpenGraph, JSON-LD and redirects. Backed by the Seo and Redirect models."
    />
  );
}
